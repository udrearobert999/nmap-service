# Clerk publishable key for the portal's authentication.
# Requires a Clerk DEVELOPMENT instance with Organizations ENABLED.
# Copy this file to .env.local and set your real key (Vite only exposes VITE_-prefixed vars).
VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxx
